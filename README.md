# Depth Alignment Project
This project aligns depth estimates from a model with ground truth COLMAP depths using a linear least-squares approach.

## Setup
Install dependencies with:
```bash
pip install -r requirements.txt
